"use client"

import { Button } from "@/components/ui/button"
import Link from "next/link"
import { Phone } from "lucide-react"

const reasons = [
  {
    title: "Rigorously Vetted, Trained Professionals",
    description:
      "Every cleaner is background-checked, reference-verified, and trained using our proprietary cleaning protocol.",
  },
  {
    title: "Our Satisfaction Guarantee: We Make It Right",
    description:
      "If you're not 100% happy, contact us within 24 hours and we’ll fix it at no cost.",
  },
  {
    title: "Upfront, Transparent Pricing",
    description:
      "You receive a detailed quote before we start. No hidden fees.",
  },
  {
    title: "Consistent Teams Assigned to You",
    description:
      "We assign consistent professionals so you build trust with the same team.",
  },
  {
    title: "Fully Licensed, Bonded & Insured",
    description:
      "Complete coverage for your peace of mind.",
  },
  {
    title: "Miami-Born, Miami-Focused",
    description:
      "We understand Miami homes and clean accordingly.",
  },
]

export default function WhyChooseUs() {
  return (
    <>
      {/* Section */}
      <section className="py-20 bg-white">
        <div className="container mx-auto px-6">
          <div className="text-center max-w-2xl mx-auto mb-16">
            <h2 className="text-3xl md:text-4xl font-bold text-primary">
              Why Choose Us
            </h2>
          </div>

          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            {reasons.map((reason, index) => (
              <div
                key={index}
                className="flex gap-4 bg-gray-50 p-6 rounded-2xl border"
              >
                <span>✅</span>
                <div>
                  <h3 className="font-bold">{reason.title}</h3>
                  <p className="text-sm text-muted-foreground">
                    {reason.description}
                  </p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* CTA */}
      <section className="py-20 bg-primary text-white text-center">
        <h2 className="text-3xl md:text-5xl font-bold mb-6">
          Ready to Get Started?
        </h2>

        <div className="flex flex-col sm:flex-row gap-4 justify-center">

          {/* FIX 1: asChild used */}
          <Button asChild size="lg" className="bg-white text-primary font-bold">
            <a href="tel:7862861851">
              <Phone className="h-5 w-5 mr-2" />
              Call Now
            </a>
          </Button>

          {/* FIX 2: Link wrapped correctly */}
          <Button asChild size="lg" variant="outline" className="border-white">
            <Link href="/contact">
              Request Quote
            </Link>
          </Button>

        </div>
      </section>
    </>
  )
}
